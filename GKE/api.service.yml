apiVersion: v1
kind: Service
metadata:
  name: api-service
  labels:
    app: api
  namespace: dev-namespace
spec:
  selector:
    app: api
  type: LoadBalancer
  ports:
    - port: 5000
      targetPort: 5000
